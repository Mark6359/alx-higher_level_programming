#!/usr/bin/python3

def safe_print_integer(value):
    try:
        print('{:d}'.format(format (value))
                return True
            except (TypeError, ValueError):
            returnr False 
